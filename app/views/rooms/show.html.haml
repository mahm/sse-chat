= form_for Message.new, url: room_messages_path(params[:id]), remote: true, id: 'new_message' do |f|
  .form-group
    = f.text_field :content, class: 'form-control', placeholder: 'Type your message'

= render 'chat', subscribe_path: room_subscribe_path(params[:id])